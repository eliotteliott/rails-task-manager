<h1>My tasks</h1>
<ul><% @tasks.each do |task| %>
  <li>
    <% if task.completed == true %>
      ☑️ <% end %>
    <%= link_to task.title, task_path(task) %>
    <%= link_to "🗑", task_path(task), method: :delete, data: {confirm: task.title} %>
  </li>
<% end %></ul>

<p> <%= link_to "+ Add a new task", new_task_path %></p>

